---FILE---
import React from 'react'
import { bem } from 'protobox/bem'

import './{{NAMEPASCAL}}.scss'

const b = bem.bind(null, '{{NAME}}')

interface {{NAMEPASCAL}}Props {
  children?: React.ReactNode
}

export function {{NAMEPASCAL}}({ children }: {{NAMEPASCAL}}Props) {
  return <div className={b()}>{children}</div>
}
---FILE---
.{{NAME}} {
  margin: 0;
}
---FILE---
export { {{NAMEPASCAL}} } from './v1/{{NAMEPASCAL}}'
